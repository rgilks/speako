# Cloudflare Pages configuration
name = "speako-web"
compatibility_date = "2024-12-01"
pages_build_output_dir = "./dist"

# Custom domain must be configured in Cloudflare Dashboard:
# Dashboard → Pages → speako-web → Custom Domains → Add: speako.tre.systems
